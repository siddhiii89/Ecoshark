rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow reads for all images
    match /donation_images/{userId}/{allPaths=**} {
      allow read: if true;
      // Only the authenticated owner can upload to their folder
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
